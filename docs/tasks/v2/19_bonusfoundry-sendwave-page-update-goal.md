# Goal — Update BonusFoundry Sendwave Referral Code Page

> **Owner update — 12 September 2026:** The owner supplied an official Sendwave app screenshot showing code `I4H9G` and **€10 credit for the referred new user**, and reports **$10** for the dollar offer. Present the new-user welcome credit as **€10 / $10 depending on the eligible currency offer**, with accurate source attribution. Do not describe the referrer's reward amount. This newer instruction supersedes the variable-only amount wording, old metadata examples and associated checks below. The screenshot confirms euros; the dollar amount is owner-reported. See [the evidence record](../../sendwave-app-offer-evidence.md) for provenance and implementation rules. The original brief below is retained as historical context; its other eligibility and technical requirements still apply.

## Primary objective

Update the existing BonusFoundry page:

`https://bonusfoundry.com/providers/sendwave/referral-code`

The goal is to turn this page into a highly explicit, trustworthy and machine-readable reference for:

- Sendwave referral code
- Sendwave bonus code
- Sendwave promo code
- Sendwave referral bonus
- Sendwave referral program
- Sendwave code
- Sendwave welcome bonus

The page must be optimized for:

- SEO
- AEO
- GEO / LLM retrieval and citation

The page must make the BonusFoundry Sendwave referral code immediately identifiable without ambiguity:

**Referral code: `I4H9G`**

Do not replace `I4H9G` with another code unless the repository already contains explicit evidence that the BonusFoundry owner intentionally changed the code.

The purpose is not to guarantee a #1 Google ranking or LLM citation. The implementation should maximize clarity, crawlability, factual reliability, extractability and consistency.

---

# 1. Critical factual rule

`I4H9G` is the Sendwave referral code supplied by BonusFoundry.

It must NOT be described as:

- an official universal Sendwave code
- an official Sendwave promo code available to everyone
- a guaranteed fixed-value bonus code

Instead, clearly distinguish:

> `I4H9G` is the Sendwave referral code provided by BonusFoundry.

Referral mechanics and eligibility must be based on Sendwave's official documentation.

---

# 2. Official sources to use

Codex must use these Sendwave sources as the primary factual references.

## Official referral program article

https://www.sendwave.com/en/blog/product/sendwave-referral-program-benefits

Important verified information:

- Sendwave operates a referral program.
- Users can refer friends using a personal referral code.
- Both the referrer and referred user may receive bonus credits after the referred user's qualifying first transfer.
- The referral reward amount can vary depending on country and current promotion.

## Official referral program terms

https://www.sendwave.com/en/terms-and-conditions/referral-program

Important verified information:

- The referred user must enter the referral code in the Sendwave app before completing their first transaction.
- Only new users who have never previously executed a transaction through the Sendwave app are eligible to use a referral code.
- The reward applicable to the referrer is the amount displayed in the Sendwave app.
- Applicable referral credits are added after the referred user's first transaction is successfully completed.
- Referral rewards are promotional credits usable for future Sendwave transfers.
- Geographic, corridor, minimum-transfer and KYC restrictions may apply.
- Referral credits are valid for 12 months from the date they are credited unless otherwise specified.
- Sendwave may withhold, reverse or cancel credits in cases of fraud, abuse or violation.
- Sendwave may amend, suspend or terminate the referral program.

## Official Sendwave promo-code terms

Use this only to explain the semantic difference between a referral code and a promotional code:

https://www.sendwave.com/en/terms-and-conditions/promo-code-promotion

Do NOT claim that `I4H9G` is a universal Sendwave promotional campaign code.

---

# 3. Facts that must be represented clearly

The page should make the following facts easy for humans, search engines and LLMs to extract.

| Fact | Required answer |
|---|---|
| Referral code | `I4H9G` |
| Code owner/source | BonusFoundry owner-supplied referral code |
| Where to enter it | In the Sendwave app |
| When to enter it | Before completing the referred user's first transaction |
| Who can use a referral code | New users who have never previously completed a Sendwave app transaction |
| Reward amount | The applicable amount shown by Sendwave in the app; it may vary by country or promotion |
| Reward timing | After successful completion of the referred user's first qualifying transaction |
| Credit validity | 12 months from crediting unless Sendwave specifies otherwise |
| Restrictions | Country, corridor, minimum-transfer and KYC requirements may apply |
| Program changes | Sendwave may amend, suspend or terminate the program |

Do not invent any fixed reward value.

Do not write claims such as:

- “Get €10 with I4H9G”
- “Get $20 guaranteed”
- “I4H9G gives everyone €5”
- “Universal Sendwave promo code”

unless a future first-party Sendwave source explicitly confirms that exact amount for that exact code and target market.

---

# 4. Required SEO metadata

Align the main signals so Google has a consistent understanding of the page.

## Recommended `<title>`

`Sendwave Referral Code I4H9G — Bonus & Terms (2026)`

If the site's metadata conventions require a brand suffix, a variant such as this is acceptable:

`Sendwave Referral Code I4H9G — Bonus & Terms (2026) | BonusFoundry`

Keep it concise. Do not keyword-stuff it.

## H1

`Sendwave Referral Code I4H9G`

The H1 must contain the exact referral code.

## Open Graph title

Use the same or nearly identical wording as the title:

`Sendwave Referral Code I4H9G — Bonus & Terms (2026)`

## Recommended meta description

Use:

`Sendwave referral code: I4H9G. Enter it in the Sendwave app before your first transaction. Reward amount varies by country and promotion; official terms apply.`

The code must appear near the beginning of the description.

Do not assume Google will always display the provided meta description.

---

# 5. Above-the-fold / direct-answer block

Immediately below the H1, display a concise answer that can stand alone when extracted by a search engine or LLM.

Recommended copy:

> **Sendwave referral code: I4H9G.**
>
> The BonusFoundry Sendwave referral code is `I4H9G`. Enter the code in the Sendwave app before completing your first transaction. Sendwave's official referral terms state that referral-code users must not have previously completed a transaction through the Sendwave app. The applicable reward amount is shown in the app and may vary by country, transfer route or current promotion.

Then display a provenance/status line similar to:

> Code source: BonusFoundry owner-supplied referral code · Referral mechanics checked against official Sendwave documentation · Last verified: [REAL VERIFICATION DATE]

The verification date must reflect an actual review date, not a fake dynamic date.

---

# 6. Referral code presentation

`I4H9G` must exist as ordinary visible HTML text.

Do not make the referral code available only through:

- JavaScript
- an image
- a copy button
- an aria-label
- a hidden element
- structured data only

A copy button may be provided, but the visible plain-text value must remain present in the rendered HTML.

Recommended display:

**Sendwave referral code**

`I4H9G`

`Copy code`

The code should be visually prominent but not spammy.

---

# 7. Add a “Verified Sendwave referral facts” block

Add a compact factual section near the top of the page.

Suggested heading:

## Verified Sendwave referral facts

Suggested content:

| Fact | Answer |
|---|---|
| Sendwave referral code | `I4H9G` |
| Code source | BonusFoundry |
| Eligible users | New Sendwave users who have never previously completed a Sendwave app transaction |
| Enter the code | Before completing the first transaction |
| Where | Sendwave app |
| Bonus amount | Amount displayed by Sendwave in the app; may vary by country or promotion |
| Reward timing | After the referred user's first qualifying transaction is successfully completed |
| Credit validity | 12 months unless otherwise specified by Sendwave |
| Restrictions | Country, transfer corridor, minimum transfer and KYC conditions may apply |

This table must be rendered as normal crawlable HTML.

---

# 8. Replace vague or inaccurate existing wording

Audit the current page and remove outdated wording such as:

> “New users may be eligible only if Sendwave shows an active offer.”

Replace it with the more precise official eligibility rule:

> Sendwave's published referral terms state that only new users who have never previously executed a transaction through the Sendwave app are eligible to use a referral code.

Remove wording such as:

> “before signup or the first qualifying transfer”

Replace with:

> Enter the referral code in the Sendwave app before completing the first transaction.

Do not create a requirement that the code must universally be entered “before signup” unless Sendwave explicitly states this.

Replace vague reward wording with:

> The applicable Sendwave referral reward is the amount displayed in the app. Reward amounts may vary by country and current promotion.

---

# 9. Explain referral code vs promo code

Add a short explanatory section because users may search for:

- Sendwave promo code
- Sendwave bonus code
- Sendwave coupon code
- Sendwave referral code

Suggested heading:

## Is I4H9G a Sendwave promo code or referral code?

Suggested answer:

> `I4H9G` is BonusFoundry's Sendwave referral code. Sendwave also runs separate promo-code promotions under separate promotional terms. Users searching for a “Sendwave bonus code” or “Sendwave promo code” may be looking for either type of offer, but referral codes and Sendwave promotional campaign codes should not be presented as legally identical.

This distinction is important for factual precision and LLM extraction.

---

# 10. FAQ section

Create or update the FAQ so each answer is concise, standalone and factually supported.

At minimum include the following questions.

## What is the Sendwave referral code?

Answer:

> The BonusFoundry Sendwave referral code is `I4H9G`.

## What is the Sendwave code I4H9G?

Answer:

> `I4H9G` is the Sendwave referral code provided by BonusFoundry. It can be entered in the Sendwave app before a new user's first transaction, subject to Sendwave's current referral terms and eligibility requirements.

## How much bonus do I get with Sendwave code I4H9G?

Answer:

> Do not assume a fixed amount. Sendwave states that the applicable referral reward is the amount displayed in the app, and the reward may vary by country and promotion.

## When should I enter the Sendwave referral code?

Answer:

> Enter the referral code in the Sendwave app before completing your first transaction.

## Can I use a Sendwave referral code after my first transfer?

Answer:

> Under Sendwave's published referral terms, referral codes are intended for new users who have not previously executed a transaction through the Sendwave app, and the code must be entered before the first transaction is completed.

## Who is eligible to use a Sendwave referral code?

Answer:

> Sendwave's published referral terms state that eligible referred users must be new users who have never previously executed a transaction through the Sendwave app. Additional country, corridor, transaction and KYC restrictions may apply.

## How long do Sendwave referral credits last?

Answer:

> Sendwave states that referral credits are valid for 12 months from the date they are credited unless otherwise specified.

## Is a Sendwave referral code the same as a promo code?

Answer:

> No. Sendwave publishes separate terms for referral codes and promotional codes. `I4H9G` is BonusFoundry's Sendwave referral code.

## Does Sendwave guarantee a fixed referral bonus?

Answer:

> No fixed universal amount should be assumed. Sendwave states that the applicable amount is shown in the app and may vary by country or promotion.

---

# 11. Official sources section

Add a prominent section titled:

## Official Sendwave sources

Include normal crawlable outbound links to:

1. Sendwave Referral Program article  
   https://www.sendwave.com/en/blog/product/sendwave-referral-program-benefits

2. Sendwave Referral Program Terms  
   https://www.sendwave.com/en/terms-and-conditions/referral-program

3. Sendwave Promo Code Terms, only where relevant to the referral-vs-promo explanation  
   https://www.sendwave.com/en/terms-and-conditions/promo-code-promotion

Use descriptive anchor text.

Do not use generic anchors such as “click here”.

---

# 12. Editorial trust and provenance

The page must make it obvious which information comes from which source.

Use language such as:

- “According to Sendwave's official referral terms…”
- “Sendwave states…”
- “BonusFoundry referral code…”
- “Last verified against Sendwave's official terms…”

Do not imply that Sendwave has endorsed BonusFoundry or the code `I4H9G`.

Keep or add an affiliate/referral disclosure if the site earns a benefit when a user uses the code.

---

# 13. Structured data

Structured data must accurately match visible page content.

Recommended types:

- `WebPage` or `Article`
- `BreadcrumbList`
- `FAQPage` only if the FAQ is visibly present on the page

Do not fabricate:

- `Product`
- `Offer`
- fake `price`
- fake bonus value
- fake `AggregateRating`
- fake review counts

Do not encode an unverified fixed Sendwave bonus amount in JSON-LD.

The FAQ JSON-LD, if used, must exactly match the visible FAQ answers.

Structured data should improve semantic clarity, not attempt to mislead search engines.

---

# 14. Entity clarity for AEO / GEO

The page should repeatedly but naturally establish these entity relationships:

- Brand/service: Sendwave
- Website/publisher: BonusFoundry
- Referral code: `I4H9G`
- Code relationship: BonusFoundry-provided Sendwave referral code
- Referral program operator: Sendwave
- Eligibility authority: Sendwave official referral terms
- Variable reward authority: Sendwave app / official terms

Ensure no paragraph creates ambiguity that `I4H9G` belongs to Sendwave globally.

Use declarative language rather than vague marketing language.

Preferred:

> The BonusFoundry Sendwave referral code is `I4H9G`.

Avoid:

> Try this amazing Sendwave promo code for huge savings.

---

# 15. Content structure

Recommended page order:

1. Breadcrumbs
2. H1: `Sendwave Referral Code I4H9G`
3. Direct-answer / hero block
4. Visible referral code + copy action
5. Verification/provenance line
6. Verified Sendwave referral facts
7. How the Sendwave referral program works
8. How to use referral code `I4H9G`
9. Eligibility
10. Reward amount and why it varies
11. Referral code vs promo code
12. Credit validity and restrictions
13. FAQ
14. Official Sendwave sources
15. Referral/affiliate disclosure
16. Last reviewed / updated information

Do not bury the code below several introductory paragraphs.

---

# 16. Suggested “How to use I4H9G” section

## How to use Sendwave referral code I4H9G

Suggested steps:

1. Download or open the official Sendwave app.
2. Create a new Sendwave account if eligible.
3. Enter referral code `I4H9G` in the app before completing your first transaction.
4. Complete the required verification steps if Sendwave requests them.
5. Review the reward and qualifying conditions shown by Sendwave in the app.
6. Complete the qualifying first transaction.
7. Referral credits, when applicable, are awarded according to Sendwave's current terms.

Do not invent a universal transfer minimum.

---

# 17. Reward section

Suggested heading:

## How much is the Sendwave referral bonus?

Suggested copy:

> Sendwave does not publish one universal referral reward amount that should be assumed for every user, country or promotion. Its official referral terms state that the applicable reward amount is shown in the Sendwave app, while Sendwave's referral-program article explains that rewards may vary by country and current promotion.
>
> For this reason, BonusFoundry should not present `I4H9G` as guaranteeing a specific euro, pound or dollar amount unless a current first-party Sendwave source explicitly confirms that amount for the relevant market.

This section is important because competitor pages often publish fixed amounts that may not be universally valid.

---

# 18. Crawlability and technical SEO

Preserve the existing URL:

`/providers/sendwave/referral-code`

Do not create an unnecessary new route.

Ensure:

- canonical points to the same preferred Sendwave referral-code URL
- page remains indexable
- no accidental `noindex`
- referral code text is server-rendered or otherwise present in crawlable HTML
- title, H1 and hero text are aligned
- official source links are normal crawlable `<a href>` links
- page is in the sitemap if the project uses one
- internal links to this page use descriptive anchor text when possible
- Open Graph metadata reflects the same core entity/code
- no contradictory Sendwave bonus values exist elsewhere in the page

---

# 19. OpenAI / LLM crawlability check

Audit `robots.txt`.

If BonusFoundry's goal is to be discoverable in ChatGPT Search, ensure the site is not unintentionally blocking:

`OAI-SearchBot`

If appropriate for the site's policy, allow:

```text
User-agent: OAI-SearchBot
Allow: /
```

Do NOT automatically change the site's policy for `GPTBot`.

`OAI-SearchBot` and `GPTBot` are different crawlers and should be treated separately.

Do not add speculative “AI optimization” directives that are unsupported by official documentation.

---

# 20. Do not prioritize llms.txt over core page quality

An `llms.txt` file is not required for Google AI features and should not replace:

- crawlable HTML
- clear entities
- factual first-party sourcing
- correct metadata
- internal linking
- indexability
- explicit direct answers

If the project already uses an `llms.txt` file, it may be updated consistently, but this is secondary.

---

# 21. Keyword / intent coverage

Cover these intents naturally without stuffing them into every paragraph:

Primary:

- sendwave referral code
- sendwave code
- sendwave referral
- sendwave referral bonus

Secondary:

- sendwave promo code
- sendwave bonus code
- sendwave coupon code
- sendwave welcome bonus
- sendwave first transfer bonus
- sendwave refer a friend

The exact code `I4H9G` should appear in prominent factual contexts.

Do not create repetitive keyword blocks solely for search engines.

---

# 22. Language

The current page should remain primarily English if that matches the existing BonusFoundry site strategy.

Do not inject large amounts of French keyword text into the English page merely to target:

- code parrainage Sendwave
- code promo Sendwave
- bonus Sendwave

If a proper French version is later created, it should use a dedicated localized URL, correct canonical setup and `hreflang` relationships.

---

# 23. Internal consistency audit

Search the repository for all Sendwave-related content and ensure there are no contradictory claims, especially:

- different Sendwave codes
- fixed bonus amounts unsupported by current Sendwave documentation
- old instructions saying the code must be entered “before signup”
- obsolete minimum-transfer claims
- misleading use of “promo code” for `I4H9G`

Where shared provider data drives multiple pages, update the canonical source of truth rather than patching one rendered page only.

However, do not make unrelated large-scale changes.

---

# 24. Preserve existing design unless needed

This task is primarily a factual/content/SEO/AEO/GEO update.

Preserve BonusFoundry's existing:

- visual identity
- layout system
- components
- responsive behavior
- accessibility conventions

Only adjust layout where necessary to make the code and verified facts substantially clearer.

Do not redesign the entire site.

---

# 25. No ranking or LLM citation guarantees

Do not add any internal or visible statement claiming that this implementation guarantees:

- Google position #1
- a featured snippet
- a specific title link
- a rich result
- ChatGPT citation
- Gemini citation
- AI Overview inclusion

Search engines may rewrite titles and snippets.

The implementation objective is to maximize relevance, clarity, trust and extractability.

---

# 26. Acceptance criteria

The task is complete only if all of the following are true.

## Referral-code visibility

- [ ] `I4H9G` appears in the HTML `<title>`.
- [ ] `I4H9G` appears in the H1.
- [ ] The first major visible content block contains `Sendwave referral code: I4H9G`.
- [ ] `I4H9G` exists as normal visible HTML text.
- [ ] The FAQ directly answers “What is the Sendwave referral code?” with `I4H9G`.

## Factual accuracy

- [ ] The page identifies `I4H9G` as the BonusFoundry-provided Sendwave referral code.
- [ ] It does not call `I4H9G` a universal official Sendwave promo code.
- [ ] It does not guarantee an unsupported fixed bonus amount.
- [ ] It states that reward amounts may vary.
- [ ] It states the official timing: enter the referral code before completing the first transaction.
- [ ] It states that eligible referral-code users must not have previously completed a Sendwave app transaction.
- [ ] It mentions potential country/corridor/KYC/minimum-transfer restrictions without inventing universal values.
- [ ] It states the 12-month referral-credit validity rule, with “unless otherwise specified”.

## Sources

- [ ] Official referral terms are linked.
- [ ] Official Sendwave referral-program article is linked.
- [ ] Promo-code terms are linked only where the referral/promo distinction is discussed.
- [ ] External links have descriptive anchor text.

## SEO

- [ ] Title and H1 are aligned.
- [ ] Meta description begins with or prominently includes `I4H9G`.
- [ ] Canonical is correct.
- [ ] Page remains indexable.
- [ ] No obvious keyword stuffing.
- [ ] No contradictory bonus claims exist on the page.

## AEO / GEO

- [ ] A concise standalone direct answer exists near the top.
- [ ] A compact facts table exists.
- [ ] FAQ answers are self-contained.
- [ ] Facts and source provenance are explicit.
- [ ] The referral code can be extracted without executing a copy-button action.
- [ ] Important content is crawlable textual HTML.
- [ ] `robots.txt` has been audited for `OAI-SearchBot`.

## Structured data

- [ ] Structured data accurately reflects visible content.
- [ ] No fake `Product`, `Offer`, `AggregateRating` or reward value was added.
- [ ] FAQ structured data, if present, matches the visible FAQ exactly.

## Quality

- [ ] Existing visual design remains coherent.
- [ ] Page works on mobile and desktop.
- [ ] No broken links.
- [ ] Build/tests/lint appropriate to the project pass.

---

# 27. Post-implementation verification

After implementing the change:

1. Run the project's relevant lint/build/test commands.
2. Inspect the rendered HTML or production-like output.
3. Confirm the exact title.
4. Confirm the exact meta description.
5. Confirm only one primary H1 is used.
6. Confirm `I4H9G` is present in server-rendered/crawlable page content.
7. Validate structured data syntax.
8. Test all official Sendwave links.
9. Search the modified page for unsupported fixed amounts such as `$5`, `$10`, `$20`, `€5`, `€10`, `€20`, etc.
10. Search the repository for contradictory Sendwave referral claims.
11. Report the files modified and summarize the factual/SEO changes.

---

# 28. Final implementation principle

The finished page should make the following statement obvious to both a human and a machine:

> **The BonusFoundry Sendwave referral code is `I4H9G`. Enter it in the Sendwave app before completing your first transaction. Sendwave's published referral terms restrict referral-code use to eligible new users who have not previously completed a Sendwave app transaction, and the applicable reward amount is determined by Sendwave and may vary by country or promotion.**

Everything else on the page should reinforce this statement without contradicting it.
